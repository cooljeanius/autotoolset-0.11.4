dnl# Environment variables that can affect the execution of the various
dnl# other macros in this package:

AC_DEFUN([LF_ENVVARS],[
  AC_ARG_VAR([LF_DEBUG_MESSAGES],[set to "yes" to print extra debug info])
])

dnl# (only used in "LF_NEED_LIBRARY_PACKAGE" so far)
