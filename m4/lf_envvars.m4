dnl# Environment variables that can affect the execution of any other
dnl# macro(s) in this package that might use it:

AC_DEFUN([LF_ENVVARS],[
  AC_ARG_VAR([LF_DEBUG_MESSAGES],[set to "yes" to print extra debug info])
])

dnl# (only used in "LF_NEED_LIBRARY_PACKAGE" so far)
